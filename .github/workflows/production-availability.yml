name: Production availability

on:
  push:
    branches: [main]
  workflow_dispatch:
  schedule:
    - cron: "17,47 * * * *"

permissions:
  contents: read
  issues: write

concurrency:
  group: production-availability
  cancel-in-progress: true

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Verify production and CDN delivery
        id: verify
        continue-on-error: true
        run: |
          set -o pipefail
          node scripts/verify-production-availability.mjs --strict | tee production-availability.log
        env:
          PRODUCTION_BASE_URL: https://sogrim-hashbon.vercel.app

      - name: Open one incident issue
        if: steps.verify.outcome == 'failure'
        uses: actions/github-script@v7
        with:
          script: |
            const title = '[Production] Availability check failed';
            const existing = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              per_page: 100
            });
            if (!existing.data.some((issue) => issue.title === title)) {
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title,
                body: `The scheduled production check failed at ${new Date().toISOString()}. Open the failed workflow run for the exact boundary.`,
                labels: []
              });
            }

      - name: Close recovered incident issue
        if: steps.verify.outcome == 'success'
        uses: actions/github-script@v7
        with:
          script: |
            const title = '[Production] Availability check failed';
            const existing = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              per_page: 100
            });
            for (const issue of existing.data.filter((item) => item.title === title)) {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issue.number,
                body: `Recovered automatically at ${new Date().toISOString()}.`
              });
              await github.rest.issues.update({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issue.number,
                state: 'closed'
              });
            }

      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: production-availability
          path: production-availability.log
          retention-days: 7

      - name: Fail after incident handling
        if: steps.verify.outcome == 'failure'
        run: exit 1
