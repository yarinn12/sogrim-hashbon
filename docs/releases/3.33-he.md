# סוגרים חשבון 3.33 (56)

## מה השתנה

- נוספה תשתית ניטור מצטברת וידידותית לפרטיות לזיהוי תקלות וחיכוך בזרימות המרכזיות.
- הניטור אינו שומר שמות, סכומים, כתובות מייל, מזהי משתמשים, שמות אירועים, קישורי הזמנה או תוכן שגיאות.
- שופרה אמינות השימוש באירוע, בהוספת הוצאה, בהזמנה ובמסך הסיכום.
- יעדי הלחיצה במנהל המשתתפים הוגדלו ל-48 פיקסלים לשימוש נוח יותר במובייל.
- נוספה בדיקת מובייל קבועה ל-Android, ל-iPhone, לטקסט מוגדל ולתצוגת reflow של 200%.
- מטמון האפליקציה עודכן כדי שכל שכבות הגרסה יהיו זמינות גם לאחר עדכון של אפליקציה מותקנת.

## מסלול בדיקה מומלץ

1. להתחבר לחשבון קיים או ליצור חשבון חדש.
2. ליצור אירוע רגיל ולהוסיף לפחות שני משתתפים.
3. להוסיף הוצאה ולבחור מי שילם ומי השתתף.
4. לפתוח את הסיכום ולוודא שההעברות ברורות.
5. לשתף קישור הזמנה ולפתוח אותו מחשבון אחר.
6. לסמן העברה כהושלמה ולבדוק שאפשר לבטל את הסימון.

האפליקציה מחשבת התחשבנות בלבד. היא אינה מחזיקה כסף ואינה מבצעת העברות בנקאיות.

## אימות החבילה

- `942/942` בדיקות אוטומטיות עברו.
- שמונה מסלולי מובייל עברו ב-Android, ב-iPhone, בטקסט מוגדל וב-reflow של 200%, כולל מסלול שימוש ראשון מלא מאירוע ריק ועד סיכום והזמנה.
- `lintRelease` ו-`bundleRelease` הסתיימו בהצלחה.
- גודל ה-AAB החתום: `13.59 MB`.
- SHA-256: `A3117B987B54BE2786A5569813D51AB3E62419BC91FA960F5CD17E26AA295A92`.

החבילה נמצאת ב-`android/app/build/outputs/bundle/release/app-release.aab`.
