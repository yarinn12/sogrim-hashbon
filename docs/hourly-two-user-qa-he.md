# בדיקת שני משתמשים מדי שעה

## הפעלה וסביבה

מתוך `C:/Users/A/Documents/Codex/2026-05-23/new-chat`:

```text
node scripts/verify-hourly-two-user-live.mjs --allow-production
```

הבדיקה פונה לאתר הייצור ול־Supabase האמיתי. היא יוצרת שני חשבונות זמניים ייעודיים בלבד, עם כתובות `qa-hourly-...@example.test`. זה אינו שימוש בירין, ירין2 או בחשבונות של חברים. אישורי הניהול הקיימים נטענים ממנגנון הסביבה הפרטי; אין להדפיס אותם, להעבירם לדפדפן או לייצר אישורים חדשים. החשבונות נכנסים בממשק עם סיסמאות זמניות שנשמרות בזיכרון בלבד.

## כיסוי

- שני הקשרים מבודדים באותו אתר: Chromium במידות Pixel ו־WebKit במידות iPhone או iPad, המתחלפים לפי שעת ההפעלה.
- כניסה והצטרפות של משתמש ב׳ דרך קישור הזמנה.
- יצירת פתק א׳ → ב׳, עריכה ב׳ → א׳ ומחיקה ב׳ → א׳, ללא רענון מקבל יזום.
- סגירת לשונית ב׳, עדכון אצל א׳ ופתיחה מחדש של ב׳.
- יצירת הוצאה אצל א׳ והופעת התיאור והסכום אצל ב׳.
- השוואת מצב שני החשבונות באמצעות קריאות מורשות רגילות, שגיאות דפדפן, חיתוך אופקי ותצלומי מסך.

הקמת האירוע משתמשת בפונקציות הלקוח הרגילות ואינה נחשבת לבדיקת כפתור יצירת אירוע. פעולות המוצר הנבדקות עצמן נעשות דרך הממשק. אין הזרקת כניסה, קריאה לפונקציות פנימיות בדפדפן, החלפת תגובות שרת או הדמיית הצלחה. רק איסוף מדדי מוצר מושבת באמצעות דגל QA קיים, כדי לא לזהם את נתוני השימוש.

זו בדיקה משני לקוחות על אותו מחשב ורשת, לא משני מכשירים פיזיים. היא אינה בודקת push נייטיבי, מקלדת iOS פיזית, כתיבה במצב offline, כל מסכי ההגדרות או כל תרחיש אפשרי. אין להסיק ממנה אפס תקלות או אפס דיליי.

## מדידה ודיווח

כל ריצה שומרת דוח ותצלומים תחת `work/hourly-two-user/`; `latest.json` הוא הדוח האחרון, וקובצי הדוחות לפי מזהה הריצה נשמרים להשוואה. הזמנים נמדדים מלחיצה עד שחלון השמירה נסגר או עד שהתוכן מופיע אצל המקבל — מדידות חוויית שימוש, לא זמן שרת בלבד. מעקב הדפדפן מוסיף תקורה.

רף בדיקה ראשוני: חלון שמירה מעל 1.5 שניות או סנכרון בין לקוחות מעל 5 שניות מסומנים לבחינה, לא מוצגים אוטומטית כאובדן מידע. P95 של ריצה קטנה אינו מדד אוכלוסייה. בדיקה שנעצרת בהצטרפות אינה מוכיחה דבר על פתקים או הוצאות באותה ריצה. אין לשנות בדיקה כדי להסתיר כשל אמיתי.

יש לסווג בקשות שנדחו: למשל בדיקת ניהול של משתמש שאינו מנהל או ניסיון הזמנה לפני כניסה אינן שקולות לדחיית שמירת מידע אחרי כניסה. אין להעלים את הרשומות מהדוח.

## בידוד וניקוי

`active.json` הוא נעילת ריצה ומניפסט מזהים בלבד, ללא סיסמאות או אסימונים. אם הוא קיים, ריצה חדשה אינה מתחילה. אין למחוק אותו כדי לעקוף ריצה פעילה.

הניקוי קורא תחילה כל חשבון וכל snapshot במזהה מדויק, מאמת כתובת QA, שייכות לריצה, בעלות, יוצר האירוע ורשימת המשתתפים. רק אז נמחקים חשבונות ונתוני הניסיון. אם נכנס גורם לא צפוי, הניקוי נעצר ושומר את המניפסט. אין מחיקה רחבה לפי תאריך או קידומת.

אם ריצה קודמת הסתיימה והניקוי נקטע, אפשר להפעיל `node scripts/verify-hourly-two-user-live.mjs --cleanup-only` לאחר אימות שאין תהליך פעיל. כשל בעלות דורש בדיקה ולא עקיפה. מחיקת נתוני QA בענן היא סופית; הדוחות והתצלומים נשארים מקומית.

## אוטומציה

האוטומציה ״בדיקת סנכרון עם שני משתמשים״ מחוברת למשימה הנוכחית ופועלת מדי שעה. היא מריצה פעם אחת, מתעדת ומשווה; מתריעה על כשל משמעותי, החמרה, התאוששות או צורך בעזרה. אין פריסה או תיקון קוד מוצר אוטומטיים במסגרת ניטור זה. נדרש שהמחשב ו־Codex יהיו זמינים. אין לולאת ניסיונות מהירה או ריצות חופפות.

## מצב אימות התשתית

שמונה בדיקות יחידה להגנות הניקוי והפרדת המדידות עברו. בשתי ריצות על PWA 472, עם שני חשבונות חדשים בכל ריצה, כניסת א׳ עברה אך כניסת ב׳ מתוך הזמנה נכשלה, עם דחיית HTTP 403 בכתיבת snapshot משותף. בריצה השנייה, קריאה ממוקדת לפני הניקוי הראתה שב׳ כבר היה חבר באירוע הקנוני, ובכל זאת מסך הכניסה נשאר עם הודעת כשל לאחר ניסיון שמירה. HTTP 401 לפני הכניסה הוא שלב נפרד ואינו ממצא התקלה.

בדיקת התחביר וחבילת בדיקות הקוד המלאה עברו: 2,173 בדיקות, ללא כשלונות, ביטולים או דילוגים. זה אינו מבטל את כשל הייצור שנמצא בשתי הריצות.

גבול הכשל המאומת: השלמת הכניסה/שמירת מצב לאחר הצטרפות בקישור. לא הוכחה עדיין הסיבה המדויקת לדחיית הכתיבה, ואין לפטור אותה ככשל אינטרנט או לייחס אותה בוודאות לתשתית הבדיקה. בהתאם לשיטת הבדיקה מקצה לקצה, הריצה נעצרת בגבול השבור ולא ממשיכה תוך עקיפת ההזמנה. הפתקים וההוצאות בתשתית השעתית החדשה טרם אומתו בריצה מלאה.

בסיום כל ריצה נוקו שני החשבונות ושלוש מטרות ה־snapshot. הדוחות `1788541644590-32204ca0.json` ו־`1788541795148-ca9a86c9.json` ותצלומי הכשל נשמרו. אין שינוי בקוד המוצר או בפריסה.

## תיקון ההצטרפות — 4 בספטמבר, הכנה ל־PWA 473

הריצה `1788543765970-64cdb323` זיהתה את סיבת ה־403: `Only an event admin can change event settings`. מיזוג קריאה ייצר שדות ברירת מחדל ריקים שלא היו ב־snapshot הקנוני. שמירתם בידי חבר רגיל התנגשה בבדיקת ההגדרות המדויקת בשרת. גבול הכתיבה שומר כעת על היעדר שדות ריקים אלה, גם בניסיון CAS חוזר. שינויים ממשיים נשמרים לבדיקה בשרת; ההרשאות לא שונו. חמש בדיקות רגרסיה נוספו, ו־2,178 בדיקות קוד עברו.

אפשר לבדוק קוד מקומי מול אותו שרת בעזרת `--local-origin=http://127.0.0.1:4197` נוסף ל־`--allow-production`. שרת הפיתוח חייב לטעון את סביבת הענן הקיימת, ולא את מצב הדמו. מצב זה מסומן מפורשות בדוח ואינו הוכחת פריסה.

בריצה המקומית `1788544610461-f12bc7ec` עברו כל שבעת שלבי המוצר, כולל השוואה מורשית של הנתונים הקנוניים ושימור מחיקת פתק בהיטל האישי. סנכרון נמדד ב־1.384–2.552 שניות; שמירת פתק ב־1.728–2.048 שניות. הדוח מסומן degraded בגלל API הניהול שאינו מוגדר בסביבה המקומית (503), לא בגלל כתיבות הסנכרון. אין להסתיר זאת או לטעון שזו בדיקת אתר הייצור.

תוקנו גם הנחות בתשתית הבדיקה: המתנה לניווט אוטומטי אחרי הזמנה, שימוש בכפתור החזרה של מסך המשתתפים, וקריאת snapshot משותף קנוני כמו בטעינת האפליקציה. snapshot אישי הוא אינדקס ומטמון להוצאות משותפות; אין להשוות אותו לבדו כאילו היה מקור האמת. הפתקים והמחיקות האישיות כן נבדקים, בנוסף להשוואת המקור הקנוני לשני המשתמשים.

אחרי פריסת PWA 473, ריצה מיידית אחת (`1788545052201-1046a675`) שמרה בהצלחה את החברות ואת ה־snapshot המשותף אך מסך הכניסה נשאר גלוי עד timeout. לא נרשמה בה דחיית כתיבה. ריצת הייצור החוזרת `1788545449011-55797a5b` עברה את כל שבעת השלבים עם שני חשבונות חדשים, כולל כניסה דרך הקישור, פתקים, הוצאה והתכנסות קנונית. לא היו שגיאות דפדפן או כשלי HTTP בלתי צפויים; זמני הסנכרון היו 1.890–2.538 שניות. כדי שכשל UI דומה לא יאבד את ההקשר שלו, דוח הכשל אוסף מעתה גם מצב gate מצומצם ללא סיסמה, אסימון, כתובת מלאה או תוכן משתמש.
